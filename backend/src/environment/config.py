'''
config.py has all the configurations of the backend system
'''
# Configurations dictionary
config = {
    'ENV': {
        'DEBUG': True,
        'PORT': 5000
    },
    'DB': {
        'DB_FILE_NAME': 'InventoryManagement.db'
    }
}
